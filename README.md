# Storyboard Timeline

Minimal prototype with Next.js frontend, FastAPI backend, Postgres, Qdrant and a crawler.

## Development

Copy `.env.example` to `.env` and run:

```bash
docker compose up -d --build
```

Visit http://localhost:3000
